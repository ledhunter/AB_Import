# 📚 Документация проекта KiloImportService

Документация по реализации сервиса импорта файлов (CSV/XLS/XLSB/XLSX) в систему Visary.

## 📂 Структура

| Файл | Содержание |
|------|------------|
| [01-alfa-core-components-api.md](./01-alfa-core-components-api.md) | 🎨 API оригинальных компонентов `@alfalab/core-components` — корректные пропы и типичные ошибки |
| [02-prototype-architecture.md](./02-prototype-architecture.md) | 🏗️ Архитектура UI-прототипа: структура папок, типизация, mock-данные |
| [03-import-flow.md](./03-import-flow.md) | 🔄 Поток импорта в UI: stages, состояния, симуляция SignalR |
| [04-css-patterns.md](./04-css-patterns.md) | 🎨 CSS-паттерны прототипа (таблица отчёта, фильтр-теги, карточки сводки) |
| [05-file-format-detection.md](./05-file-format-detection.md) | 🔍 Автоопределение формата файла по расширению |
| [06-import-types-registry.md](./06-import-types-registry.md) | 📋 Реестр типов импорта (открытый список через Select) |
| [07-import-datetime-metadata.md](./07-import-datetime-metadata.md) | 🕐 `startedAt`/`completedAt` как информационные поля (не вводятся пользователем) |
| [08-visary-api-integration.md](./08-visary-api-integration.md) | 🔌 Интеграция с Visary ListView API (proxy, Bearer, маппинг, тесты, многослойное логирование) |
| [09-lazy-loaded-select.md](./09-lazy-loaded-select.md) | 🎯 Lazy-load паттерн для Select (`idle/loading/success/error`, `onOpen`, `AbortController`) |
| [10-listview-library.md](./10-listview-library.md) | 🧰 Библиотека методов Visary ListView (generic-ядро + per-entity адаптеры, как добавить новый эндпоинт за 3 шага) |
| [11-react-refs-discipline.md](./11-react-refs-discipline.md) | 🪝 Дисциплина `useRef` в React 19: правила записи `ref.current`, паттерн «latest value» через `useEffect`, типичные ошибки |
| [12-ef-core-migrations.md](./12-ef-core-migrations.md) | 🗄️ EF Core миграции для service-db: guard `EF.IsDesignTime`, `MigrationsHistoryTable`, partial unique index, команды dotnet-ef |
| [13-vite-proxy-backend.md](./13-vite-proxy-backend.md) | 🔌 Vite proxy для собственного backend: префиксы `/api/imports`, `/hubs` с `ws: true`, факторинг через `backendProxy()` helper |
| [14-imports-backend-integration.md](./14-imports-backend-integration.md) | 🔄 Полный контур UI ↔ backend: importsService + importsHub + useImportSession + DTO ↔ UI mapper |
| [15-signalr-progress.md](./15-signalr-progress.md) | 📡 SignalR прогресс импорта: `StageProgress` с троттлингом ≈50/файл, `JoinSession`, autoReconnect, защита от старых событий |
| [16-import-cancellation.md](./16-import-cancellation.md) | 🛑 Отмена сессии: Singleton-реестр `CancellationTokenSource`, `MarkCancelledAsync` с независимым ct, парсеры с `catch (OCE) { throw }` |
| [17-backend-tests-xunit.md](./17-backend-tests-xunit.md) | 🧪 Backend xUnit-тесты: in-memory EF, SkippableFact для ClosedXML/SkiaSharp, паттерн уникальной БД на тест |
| [18-projects-cache.md](./18-projects-cache.md) | 🗂️ Кэш проектов Visary + поиск-as-you-type, **probe-then-sync** стратегия (sync только если кэш пуст), graceful fallback при Visary outage |
| [19-net10-deployment-gotchas.md](./19-net10-deployment-gotchas.md) | 🐛 5 граблей при первом деплое .NET 10 + Alpine: addgroup, Swashbuckle 10.1.7, JsonDocument в InMemory, TLS revocation, varchar(64) → 255 |
| [20-select-with-search.md](./20-select-with-search.md) | 🔍 Alfa Select + showSearch + динамические опции: пиннинг выбранной опции, очистка search на onChange |
| [21-sites-by-project.md](./21-sites-by-project.md) | 🏗️ Получение объектов строительства по проекту: специальный эндпоинт `/onetomany/Project`, AssociationFilter, полный список колонок |
| [22-update-finishing-material.md](./22-update-finishing-material.md) | 🏗️ Обновление типа отделки объекта строительства через Visary CRUD API |
| [23-finmodel-import.md](./23-finmodel-import.md) | 📊 Импорт "Финмодель": обновление параметров объекта строительства (тип отделки) из Excel |
| [24-finmodel-testing-and-fixes.md](./24-finmodel-testing-and-fixes.md) | 🧪 Тестирование и исправление ошибок: in-memory БД, Docker, типичные проблемы при добавлении мапперов |
| [25-ui-project-options-display.md](./25-ui-project-options-display.md) | 📋 Отображение проектов в Select без лишних символов: убран `code`, одни и те же файлы можно загружать несколько раз |
| [26-troubleshooting.md](./26-troubleshooting.md) | 🛠️ Решение проблем: запуск backend, проверка БД, отладка импорта "Финмодель" |
| [27-checklists.md](./27-checklists.md) | ✅ Чек-листы: запуск цикла, добавление маппера, деплой |
| [50-visary-api-new-methods.md](./50-visary-api-new-methods.md) | 🔌 Новые методы Visary API Client: 11 ListView + 12 CRUD методов для Room, Deal, ТЭП, ЗУ, ДДУ, Секция, Организация, PercentBet, Project |
| [51-sites-sync-bugs-and-token-update.md](./51-sites-sync-bugs-and-token-update.md) | 🐛 Два бага синхронизации объектов: `new HttpClient()` в Docker + отсутствие `/api/sites` в Vite proxy; обновление Bearer token |
| [53-visary-api-schema-audit.md](./53-visary-api-schema-audit.md) | 🔍 Snapshot 19 сущностей Visary API в Postgres (`visary_api.fields`), сравнение с DTO, 442 поля; скрипт `audit-visary-api.ps1` |
| [54-visary-token-hot-reload.md](./54-visary-token-hot-reload.md) | 🔐 Bearer-токен Visary — единый источник в корневом `.env` (gitignored): docker-compose, Vite (`envDir: '..'`), backend (`DotEnvLoader`), live-тесты. Hot-reload отдан ради SSOT |
| [55-visary-proxy-controllers.md](./55-visary-proxy-controllers.md) | 🔌 `/api/visary/*` контроллеры: registry-pattern для 8 справочников, явные actions для 11 основных сущностей; добавление нового справочника = 1 строка |
| [56-visary-dto-deserialization-pitfalls.md](./56-visary-dto-deserialization-pitfalls.md) | ⚠️ Ловушки десериализации: `Status`/`RoomCategory`/`MainSource` приходят разными типами → `JsonElement?`; `*Raw` vs `*Full` для listview vs crud |
| [57-visary-api-testing.md](./57-visary-api-testing.md) | 🧪 Три уровня тестов Visary API: 39 контракт-тестов клиентов, 18 тестов контроллеров, 38 live smoke-тестов с автоматическим skip при истёкшем токене |
| [52-project-dropdown-empty-query.md](./52-project-dropdown-empty-query.md) | 🎯 Dropdown «Проект»: пустой запрос возвращает первую страницу кэша (probe-then-sync не блокируется при недоступном Visary) |
| [60-select-desktop-vs-responsive.md](./60-select-desktop-vs-responsive.md) | 🎨 `SelectDesktop` вместо `SelectResponsive`: всегда классический dropdown, не Bottom Sheet на узких экранах |
| [61-finmodel-file-level-column-error.md](./61-finmodel-file-level-column-error.md) | 🛑 FinModel: file-level ошибка `column_not_found` со списком обнаруженных колонок вместо row-spam на 2782 строки |
| [62-vertical-keyvalue-layout.md](./62-vertical-keyvalue-layout.md) | 🎨 Вертикальный key-value layout (`Inputs C/H+`) + управляющий лист `Control` для числа этапов — `FileLayoutHint.KeyValueVertical` |
| [63-site-finishing-material-update-crud.md](./63-site-finishing-material-update-crud.md) | 🔌 Обновление типа отделки через PATCH `/crud/constructionsite/{id}?forceUpdate=false` (вместо PUT/listview → 405 / 500) |
| [64-dynamic-finishing-material-dictionary.md](./64-dynamic-finishing-material-dictionary.md) | 🔌 Справочник «Тип отделки» из Visary (`listview/finishingmaterial`) вместо хардкода Title→ID — переиспользуемый метод в `IListViewClient` |
| [65-merge-integration-with-shared-helpers.md](./65-merge-integration-with-shared-helpers.md) | 🔧 Merge feature-ветки с main: убираем дубликаты `private GetCrudAsync` / DTO в пользу `GetCrudByIdAsync<T>` + `Dto/Generated/`, единый namespace `VisaryMnemonics`, обновление доков |
| [66-finmodel-estate-class.md](./66-finmodel-estate-class.md) | 🏘️ Финмодель: добавлен параметр «Класс жилья» (Visary `EstateClass`) — `UpdateSiteEstateClassAsync` + динамический справочник через `ListEstateClassesAsync`, обобщённые helpers `TryLoadDictionaryAsync` / `ResolveValue` |
| [67-finmodel-indicators.md](./67-finmodel-indicators.md) | 📊 Финмодель: добавлен 3-й тип параметров — показатели (`ConstructionSiteIndicator` + Value по стадии). Подключены: «Площадь застройки», «Плотность застройки» (Stage=50 «Экспертиза»). Декларативный `Indicators[]` (расширяется одной строкой), обобщённый `ApplyIndicatorAsync`, `FilterByStringContains` + `Trim()` для Title с хвостовыми пробелами, `TryParseFlexibleDouble` |
| [68-rooms-import.md](./68-rooms-import.md) | 🏠 Импорт «Помещения» (`rooms`) по «Пример импорта.xlsx» / «Единая форма 3»: per-row сверка выбранного ОКСа (НПС+Этап+опц. РНС), группировка по листу = тип, fallback Kind по имени листа («Квартиры»→«Квартира»), `Section.Type=МЖД` дефолт, `RoomKind` из живого Visary API |
| [69-finmodel-address.md](./69-finmodel-address.md) | 🏠 Финмодель: добавлен параметр «Строительный адрес» (`Address`) — простой строковый атрибут Site через `UpdateSiteAddressAsync`, без справочника (отличие от FinishingMaterial / EstateClass — `Address = "string"`, не `VisaryRef`) |
| [70-wbs-api-foundation.md](./70-wbs-api-foundation.md) | 🏛️ WBS (ИСР) API клиент v0.1: `Wbs` мнемоника, `WbsRaw` / `WbsCreateRequest`, `CreateWbsAsync` + `GetWbsByProjectAsync`, smoke-тесты на проекте 4584. Code присваивается сервером (Глава 1 → 1.1, 1.2…). Маппер бюджета — следующий шаг |
| [71-finmodel-budget-import.md](./71-finmodel-budget-import.md) | 💰 Импорт «Финмодель → Себестоимость» (WBS v0.2): `BudgetSectionHint` для парсера, `BudgetReferenceProvider` (Title→Code, ~100 статей в коде), идемпотентный `ApplyBudgetAsync` (find/create/patch) + `PatchWbsAsync` `forceUpdate=true`. Повторный импорт не плодит дубликатов; суммы PATCH-аются |
| [72-multi-sheet-import.md](./72-multi-sheet-import.md) | 🗂️ Сквозной обзор многолистового XLSX-импорта — 5 мест кода (парсер, БД, пайплайн, маппер, UI), которые должны быть согласованы; миграция `(SessionId, Sheet, SourceRowNumber)`, per-sheet прогресс, race condition `setSession(prev =>)` |
| [73-import-history-page.md](./73-import-history-page.md) | 🗂️ Страница «История импортов»: `GET /api/imports` (skip/take/status/importTypeCode) + read-only детальный просмотр (переиспользует `SessionSummary` + `SessionRowsTable`), отдельный `useImportSessionDetail` без SignalR. Сброс `skip=0` при смене фильтров |
| [74-import-pdf-export.md](./74-import-pdf-export.md) | 📄 Экспорт сессий в PDF: чекбоксы в `HistorySessionsTable` (Set, не Array), `POST /api/imports/export-pdf` → `PDFsharp` + `PDFsharp-MigraDoc` (MIT). FontResolver регистрируется один раз под lock; в Dockerfile `apk add ttf-dejavu fontconfig` для кириллицы. Frontend: `response.blob()` + `<a download>` + `URL.revokeObjectURL` |
| [75-projectmanagement-developer-link.md](./75-projectmanagement-developer-link.md) | 🏗️ Привязка Организации-Застройщика к объекту через `projectmanagement`: 5-шаговый flow (1) PIN→org, (2) PM на сайте, (3) поиск в проекте через `/onetomany/Project` + `["Organization","contains","ID:{id}"]`, (4) max(ID) при нескольких или CREATE, (5) link. Новые методы `GetProjectManagementsBy{Site,Project}Async`/`CreateProjectManagementAsync`/`LinkProjectManagementToSiteAsync` в `Visary.Api.Client`. `FilterByRefIdContains` helper — для VisaryRef-полей |
| [76-share-agreement-dedup.md](./76-share-agreement-dedup.md) | 🔁 Дедупликация ДДУ: бизнес-ключ = (Number, RoomKindRef, ConditionalNumber, StageNumber, ProjectNumber). `FindShareAgreementsAsync` (глобальный listview), `ShareAgreementPatchRequest` расширен полями Room/RoomKindRef/ConditionalNumber/StageNumber/ProjectNumber для «реанимации» orphan-ДДУ. **v1.1 (2026-05-22)**: добавлен 3-й шаг — loose-find без `Stage`/`Project` + orphan-only фильтр (`a.Room?.ID is null`). Раньше strict-only поиск отсекал orphan'ы (у которых Stage/Project NULL), и каждый импорт плодил ДДУ-дубликат. Anti-pattern #2 расширен: loose без orphan-only filter «угоняет» чужой ДДУ из соседнего проекта/этапа. +2 теста |
| [77-room-uniqueness-building-section.md](./77-room-uniqueness-building-section.md) | 🚪 Ключ уникальности Room = Section × Kind × Number × **BuildingSection** («Подъезд/Секция» из файла). Без этого два подъезда с одним номером PATCH-или друг друга. Match с `Trim() + OrdinalIgnoreCase` для нормализации null/пробелов |
| [78-budget-xlsx-export.md](./78-budget-xlsx-export.md) | 📤 **Заменяет [70](70-wbs-api-foundation.md)/[71](71-finmodel-budget-import.md):** CRUD-путь WBS отключён (Visary 500 на `listview/wbs/onetomany/ConstructionProject`). Бюджет «Финмодели» выгружается XLSX по эталону `Бюджет_А4.1`: embedded resource → ClosedXML копия → подмена сумм в C/D, агрегация Глава/Раздел снизу вверх. **v1.3 (2026-05-14)**: полное дерево для Главы 1 (отсутствующие = 0); Главы 2/3 свёрнуты до строки самой главы (`CollapsedChapterCodes`); распознавание главы по «Глава N» префиксу; chapter-direct override итоговой суммы из строки «Итого…» файла (для Глав 2/3, где статьи в файле ≠ справочнику); закрытие главы после первого «Итого…» (избегаем двойного учёта повторов под «Этап 2» / «фактические»); двусторонний fuzzy-match Title (forward + reverse-prefix-in-chapter). `GET /api/imports/{id}/budget-xlsx` |
| [79-rooms-import-validation-and-fileupload-ux.md](./79-rooms-import-validation-and-fileupload-ux.md) | 🧹 Импорт «Помещения»: нормализация `RoomNumber` через `ExtractDigitsOnly` («п1»→«1», «12А»→«12»; отличается от `ExtractNumericPart` тем, что точки/запятые тоже отбрасываются); `required_missing` для «Количество комнат», если Kind=«Квартира» (проверка после резолва title из колонки или имени листа); UI — явные кнопки «Удалить файл» / «Выбрать другой» под `FileUploadItem`, один `<input>` вынесен наружу + сброс `value` для повторного выбора одноимённого файла |
| [82-visary-file-storage-upload.md](./82-visary-file-storage-upload.md) | 📤 Автозагрузка XLSX-бюджета в файловое хранилище Visary + создание `typedimportwbs` (TypedJournal). **v1.1 (2026-05-15)**: drive/dir больше **не** из конфига — парсятся из `ConstructionProject.ProjectFolder` выбранного проекта (формат «driveId,directoryId», напр. «32,40110»); `BudgetUploadOptions` усохло до одного `ImportType`; добавлен шаг 0 `GetProjectByIdFullAsync` + `ParseProjectFolder` без fallback (пустой/невалидный → 400). **v1.2 (2026-05-15)**: `ExtractLinkToken` теперь ищет свойство `Link/Result/Value/Token` **case-insensitive** — Visary возвращает `{"Link":"…"}` в PascalCase, прежний `TryGetProperty("link")` это не находил, fallback отдавал backend-у сериализованный JSON целиком, и Visary валился с «not a valid Base-64 string». |
| [81-xlsx-external-links-strip.md](./81-xlsx-external-links-strip.md) | 🔗 Парсинг XLSX с external workbook links (формулы/defined names с URL вида `'https://…/[file.xls]Sheet'!…`): catch «Unable to determine token» → zip-уровень cleanup (`xl/externalLinks/*`, `<externalReferences>`, external `definedName`, `externalLink` rels, **v1.1 +`<f>...URL...</f>` в `xl/worksheets/sheet*.xml`**) → retry. **v1.2**: retry поднят с ctor на весь блок парсинга (ленивый парсинг формул). **v1.3**: `byte[]` вместо `MemoryStream` — ClosedXML после неудачи закрывает stream, поэтому второй attempt должен получить свежий MS поверх неизменяемого источника. Кэшированные `<v>` остаются — данные читаются. |
| [80-multi-sheet-report-end-to-end.md](./80-multi-sheet-report-end-to-end.md) | 🗂️ Дополнение к [72](72-multi-sheet-import.md): сквозной проброс `Sheet` от БД до UI отчёта. `ImportsController.GetReport` возвращает `Sheet` для строк и ошибок и сортирует по `(Sheet, SourceRowNumber)`; `ApiImportRow/Error.sheet` + `UiReportRow/Error.sheet`; в `toUiReport` композитный ключ `rowKey(sheet, rowNumber)` + сортировка `localeCompare(_, 'ru')`. В `SessionRowsTable` каждый лист рендерится в своём `<tbody>` с заголовком «Лист: …», ключ строки `${sheet}::${rowNumber}` (без React-warning о дубликатах) |
| [87-report-pagination.md](./87-report-pagination.md) | 📄 Пагинация построчного отчёта: `useImportSession`/`useImportSessionDetail` принимают `skip` и экспортируют `loadReportPage(skip)`; `SessionRowsTable` рисует `@alfalab/core-components/pagination` + range-индикатор «{from}–{to} из {total}». `REPORT_PAGE_SIZE=100` синхронизирован с backend. ⚠️ При смене страницы сбрасываем локальный фильтр (`setFilter('all')`), иначе пользователь попадёт в пустой набор |
| [86-rooms-dedup-pre-check.md](./86-rooms-dedup-pre-check.md) | 🔁 Pre-check дедупа для Section/Room/ShareAgreement в `RoomsFormImportMapper.ApplyAsync`. ДДУ: **двухступенчатый** поиск — сначала `GetShareAgreementsByRoom(roomId, null)` (без серверного фильтра — Visary `=` чувствителен к whitespace) + локальный Trim()+OrdinalIgnoreCase, потом глобальный `FindShareAgreementsAsync` для orphan-кейсов. Section/Room — Trim+OrdinalIgnoreCase везде. Метки журнала различают 4 исхода для ДДУ: «найден в помещении», «найден глобально (привязан)», «найден (не создан)», «создан» |
| [85-per-row-action-log.md](./85-per-row-action-log.md) | 📒 Построчный журнал Apply: маппер копит per-row метки («Корпус создан», «Помещение обновлено», «ДДУ найден (не создан)», «Застройщик переиспользован») в `ApplyResult.RowActions` → `StagedRow.Actions` (jsonb). `GET /api/imports/{id}/report` отдаёт массив `actions` для каждой строки; `SessionRowsTable` рендерит зелёный блок `.messages--success`. Сохранение — **всегда**, даже если массовый Apply упал (частичный журнал — диагностически ценный). Ключ — `(Sheet, SourceRowNumber)`, совпадает с уникальным индексом таблицы. Опциональное поле — старые мапперы не ломаются |
| [84-rooms-count-normalization.md](./84-rooms-count-normalization.md) | 🔢 Импорт «Помещения»: «Колич. комнат» нормализуется по первой непрерывной группе цифр — `1 к.`, `1 к`, `п1`, `1п`, `10 к`, `3-к` → 1, 1, 1, 1, 10, 3. `студия`/`—` → `null` без `invalid_number`. Алиасы расширены (`Колич комнат` без точки — реальный заголовок в файлах заказчика). `internal static ExtractFirstRunOfDigits` + 22 unit-теста. ⚠️ Берём ПЕРВЫЙ run, не клеим: `1 к. 2` → 1, не 12 |
| [90-rooms-skip-unknown-kind-sheets.md](./90-rooms-skip-unknown-kind-sheets.md) | 🎯 Импорт «Помещения»: маппер обрабатывает только листы, чьё имя резолвится в `RoomKind` живого справочника Visary (`ResolveKindBySheetName` — точный матч + plural-trim для «Квартиры»/«Машиноместа»). «Кв_01.04.26», «Кв_01.03.26 (2)», «Кв_01.02.26 (2)» — исторические снапшоты — фильтруются ДО основного валидационного цикла. Если **все** листы отфильтрованы → file-level `no_data`. Substring-fallback сознательно НЕ используется (иначе «Кв_» совпало бы с «Квартира»). Третий слой защиты после `HeaderAnchors` ([83](83-rooms-shifted-header-row.md)) и hidden-фильтра ([88](88-xlsx-skip-hidden-sheets.md)) |
| [89-mappedrow-sheet-invariant.md](./89-mappedrow-sheet-invariant.md) | 🧩 `MappedRow` теперь несёт non-nullable `Sheet` (маппер обязан заполнять). Пайплайн использует `mr.Sheet` вместо `parseResult.Rows[i].Sheet` — иначе при тихом skip-е сводных строк индексы расходятся и в БД летит чужой лист → `23505 duplicate key` по `IX_staged_rows_ImportSessionId_Sheet_SourceRowNumber`. `totalsBySheet` для UI-прогресса считается из `validation.Rows`. `parsedByKey` lookup `(Sheet, SourceRowNumber) → ParsedRow` для восстановления `RawValues.Cells`. Сломал прод на `UC9NVP_Ежевика_01.04.2026.xlsx` (3 visible rooms-листа, 6675 silent-skip-ов сводных строк) |
| [88-xlsx-skip-hidden-sheets.md](./88-xlsx-skip-hidden-sheets.md) | 🙈 `XlsxParser` пропускает `XLWorksheetVisibility.Hidden`/`VeryHidden` — для `Tabular` (до `RangeUsed`/анкоров), `KeyValueVertical` (только видимые в поиске и в подсказке «доступные листы»), `ReadStageCount` (`Control`). Все листы скрыты → file-level error «нет видимого листа». Защита от Финмодель-шаблонов со спрятанным `Inputs` и от пользовательских файлов со «спрятанными черновиками» в обычном Tabular-импорте |
| [83-rooms-shifted-header-row.md](./83-rooms-shifted-header-row.md) | 🪞 Импорт «Помещения»: шапка не обязана быть в строке 1 (как в «Ежевика короткая 1.xlsx» — там она в строке 5 под заголовком документа и коэффициентами). Добавлен `Tabular(HeaderAnchors)` — парсер сканирует первые 30 строк и берёт ту, где встретилось ≥2 анкоров. **Strict-skip**: если анкоры заданы, но в листе не нашлись — лист пропускается ЦЕЛИКОМ (так фильтруются вспомогательные листы «Общий график»/«Итог»/«План»). `RoomsFormImportMapper` пробрасывает анкоры (`ПИН застройщика`, `Номер разрешения`, `Номер проекта`, `Этап`, …) и тихо пропускает сводные строки (`ИТОГО`, `Сумма…`), где все идентификационные поля пустые — иначе они порождали `site_mismatch` на каждую квартиру. `SourceRowNumber` теперь абсолютный Excel-номер |
| [91-finmodel-chapter1-schedule.md](./91-finmodel-chapter1-schedule.md) | 📊 ГФ Главы 1 (`CostItem`): из листа `Inputs` (шапка дат в r7, статьи в r481-483, колонки H..CU) импорт строит график финансирования. Новая сущность Visary `costitem` (CRUD/listview), парсер-хинт `ChapterScheduleHint`, маппер берёт **только Этап 1**, алиас «Прочие затраты»→1.8, ×1000 в рубли, идемпотентный POST/PATCH/skip по `(WBSID, PlanPeriod.Start)`. Per-cell сообщение «для ячейки H481 не была добавлена информация для ГФ, тк статья 1.1 отсутствует в ИСР» при отсутствии WBS-узла |
| [92-keyvaluevertical-stagecount-fix.md](./92-keyvaluevertical-stagecount-fix.md) | 🪞 `XlsxParser.ParseKeyValueVertical` со `StageCount=N` обязан эмитить ровно N `ParsedRow` — даже если правые колонки в `RangeUsed` пусты. Раньше `Math.Min(lastCol, N)` срезал шаблон с 2 этапов до 1, когда Этап 2 пустой. Заодно — зачистка устаревших `FinModelBudgetTests`: chapter-direct итог теперь второй MappedRow, reverse-prefix матчит «Прочие затраты»→1.8 в Главе 1, 5 тестов CRUD-пути WBS удалены (CRUD-flow отключён, см. [78](./78-budget-xlsx-export.md)). **Урок**: при отключении flow тесты на flow обновлять в той же ветке |
| [93-security-audit-workflow.md](./93-security-audit-workflow.md) | 🔒 Full-repo security audit workflow (дополнение к встроенному `/security-review`, который покрывает только pending changes). 7 этапов: secrets/git-history → deps (.NET/npm/images) → backend (11 OWASP-подэтапов) → frontend (XSS/token-in-bundle) → DB privileges → Docker hardening → Visary integration. Project-specific риск-зоны R1-R10 (JWT в Vite-bundle, HAR с токенами в `Context/`, SSRF в `/api/visary/*`, XXE/Zip-Slip в XLSX, SignalR `JoinSession` ownership, jsonb→XSS sink в отчёте, etc.). Инструменты (gitleaks, trufflehog, semgrep, trivy, ZAP), CVSS-lite severity rubric, шаблон отчёта о уязвимости |
| [95-history-project-filter-and-collapsible-sheets.md](./95-history-project-filter-and-collapsible-sheets.md) | 🗂 История импортов — фильтр «Проект» в `GET /api/imports` (LEFT JOIN на `CachedProjects` → `projectName`), `useBackendProjects` в `HistoryFilters` со sticky-опцией выбранного. Размер страницы отчёта 100 → **50** (`REPORT_PAGE_SIZE`, дефолт `take` в `GetReport`). Сворачиваемые листы: backend принимает `excludeSheets[]` и пересчитывает `rowsPagination.total`, отдаёт `sheetTotals[]` по ВСЕМ листам сессии; UI рисует `<button>`-аккордеон в `SheetHeaderRow`, idempotent-guard через `lastSentRef` от двойной загрузки на первом render'е. Свёрнутые листы рисуются в конце таблицы с counter из `sheetTotals` (не из локальных групп — иначе «0 стр.»). `excludeSheetsRef` сохраняется между пагинацией/reload, сбрасывается при смене `sessionId` в истории. **v1.1 (2026-05-20)**: фикс `TypeError: Cannot read properties of undefined (reading 'value')` в `HistoryFilters.tsx:122` — Alfa-Select `searchProps.onChange` принимает уже извлечённую `string`, не SyntheticEvent (`(value) => …`, не `(e) => e.target.value`); `placeholder` — через `componentProps`. Empty-state в `HistorySessionsTable` теперь различает «нет сессий вообще» и «нет под текущие фильтры» (новый prop `hasActiveFilters`), даёт подсказку сбросить фильтры. Doc [20-select-with-search.md](./20-select-with-search.md) дополнен «Ошибкой 4» с симптомом и правильной сигнатурой |
| [98-action-filters-and-single-sheet-collapse.md](./98-action-filters-and-single-sheet-collapse.md) | 🎛 Отчёт импорта: в `.filter-tags` добавлены 3 action-фильтра — **Созданные** / **Обновлённые** / **Пропущенные** по меткам `row.actions`. **v1.1**: regex сужен до главной сущности (`/Помещение создан/`, `/Помещение обновлен/`, `/Без изменений\|пропуск/`) — категории взаимоисключающие, сумма ≤ Applied (раньше с `/создан/`/`/обновлен/` строка с PATCH Room + CREATE SA попадала и в Created, и в Updated → 27+49+1 > 50). **v1.2**: счётчики переехали на backend — `GET /report` возвращает `actionTotals` по ВСЕЙ сессии (substring-агрегат по `Actions` jsonb в `ImportsController.GetReport`), фильтры показывают «187 Created» вместо «27 из видимой страницы». **v1.3 (2026-05-22)**: status-фильтры (`Все`/`Валидные`/`С ошибками`/`Применённые`/`Не применилось`) тоже переехали на backend — `GET /report` возвращает `statusTotals` (one GroupBy по `StagedRow.Status`, `valid = Valid+Applied`). Раньше «С ошибками 22» в верхней панели и «С ошибками 0» в фильтре расходились (фильтр считал по 50 видимым строкам). Frontend fallback на page-level при отсутствии `statusTotals` — для совместимости со старым backend'ом. **Параллельно**: `canCollapseAny = allSheets.length >= 1` (было `> 1`) — теперь можно свернуть и единственный лист (типичный кейс RoomsForm: «Квартира» на 6000+ строк); `null`-листы по-прежнему non-toggle |
| [97-rooms-apply-tests-and-budget-uploader-interface.md](./97-rooms-apply-tests-and-budget-uploader-interface.md) | 🧪 Тесты `RoomsFormImportMapper.ApplyAsync` (4 integration-теста: первый run / diff-skip / PATCH при изменении площади / multi-row sheet) + рефакторинг `BudgetVisaryUploader` → `IBudgetVisaryUploader` ради тестируемости `FinModelImportMapper`. **Grabble 1**: `Guid.NewGuid()` в делегате `services.AddDbContext(o => o.UseInMemoryDatabase($"X_{Guid.NewGuid()}"))` создаёт НОВУЮ in-memory БД на КАЖДЫЙ scope — Apply №2 не видит snapshot Apply №1, diff-skip не работает в тестах (production OK). **Grabble 2**: outer/inner репо имеют одинаковый `name: kilo-import` в compose — контейнеры с теми же именами. `docker inspect ... working_dir` показывает, кто реально владеет; outer-ownership → backend env из outer `.env`, образы из outer-кода → симптомы: UI без листов/Actions, незнакомые алиасы из `RoomsImportMapper.cs` (outer-имя). Лечение — `build` + `--force-recreate` из inner |
| [96-rooms-incremental-parallel-apply.md](./96-rooms-incremental-parallel-apply.md) | ⚡ Apply импорта «Помещения» переписан под инкрементальный + параллельный режим. Новая таблица `import.room_apply_snapshots` хранит `MappedHash` SHA256 от канонизированных `MappedValues` по бизнес-ключу (Site+Sheet+Section+Kind+RoomNumber+BuildingSection); pre-load в `ConcurrentDictionary` на сессию → diff-skip без БД-запросов на каждой строке (повторный импорт того же файла = ноль `PatchRoom`/`PatchShareAgreement`). Apply: ① pre-load snapshots → ② РНС в Site → ③ Sections **sequential** find-or-create → ④ Developer link **sequential** → ⑤ `Parallel.ForEachAsync` по группам (Sheet, Section) с `MaxDegreeOfParallelism = min(8, ProcessorCount, N_groups)`; внутри группы sequential → ⑥ `UpsertBatchAsync` один SaveChanges на партию. Lock-free: `Interlocked.Increment` для applied/skipped, `ConcurrentBag<RowError>`, `ConcurrentDictionary<(Sheet,Row), List<string>>` с локальным `lock(list)` на Add. Маппер Singleton, store Scoped — captive через `IServiceScopeFactory` (паттерн `BudgetVisaryUploader`). `HashedMappedFields` фиксирован отдельно от `MappedValues` — добавление диагностического поля в маппер не ломает diff-skip. `BuildKey` нормализует `Trim()+ToLowerInvariant` и `kindId ?? 0` (Postgres NULL ≠ NULL в unique index) |
| [99-finmodel-organization-link.md](./99-finmodel-organization-link.md) | 🏢 Финмодель → раздел «Основные данные»: по ИНН найти/создать `organization` (POST `/crud/organization` если по `ClientID` не нашлось), затем найти/переиспользовать/создать `projectmanagement` (Заёмщик/Застройщик) и привязать к выбранному `constructionsite` через manytomany-link. Пара колонок «ИНН» + «Заемщик/Застройщик» — **опциональная** (старые шаблоны без раздела продолжают работать; одна колонка из пары → row-error). PM-поиск в проекте без фильтра по `Role.ID` — переиспользуем любую существующую запись (max ID), при создании Role=`Developer (10)`. Идемпотентность: pre-check `GetProjectManagementsBySiteAsync` → skip если уже привязана. 9 новых тестов |
| [103-cancel-and-stuck-status.md](./103-cancel-and-stuck-status.md) | 🛑 «Отменить» не работает на зависших сессиях. Корень: (1) Upload-background проглатывал unhandled exception без обновления статуса в БД — сессия оставалась `Validating`; (2) Cancel-endpoint переводил в `Cancelled` только статусы `Pending/Validated`, игнорируя `Parsing/Validating/Applying`. Новый public `ImportPipeline.ForceFinalizeAsync(sessionId, finalStatus, reason, ct)` (только в Failed/Cancelled/Applied, гард от повторной финализации, SignalR-нотификация). Upload-background: при `catch(Exception)` → `ForceFinalize(..., Failed)`. Cancel-endpoint: при non-final-статусе без активного CTS → `ForceFinalize(..., Cancelled)`. Используем `default`-токен, чтобы запись в БД не упала на Cancel-нутом ct. |
| [102-pagination-totalarea-excel-errors.md](./102-pagination-totalarea-excel-errors.md) | 🧹 Три фикса после Репино-Парк: (1) пагинация — убран orphan-rows push в `toUiReport` (страница раздувалась до `take + N_errors`, скрин «1–693 из 1735»); (2) Excel-ошибки `#N/A/#REF!/#VALUE!/#NAME?/#NUM!/#DIV/0!/#NULL!/#GETTING_DATA` в `№ ДДУ/НПС/Этап/Секция/Этаж/Подъезд/PIN` обнуляются в Validate — иначе Apply создавал SA с `Number="#N/A"` и реанимировал тот же глобальный ДДУ на каждой строке → Visary 500; (3) `TotalArea` для нежилых читается из новой колонки «Общая площадь, кв.м.» (`TotalAreaAliases`) с fallback на ProjectArea — раньше уходил только `"ProjectArea":0`. +3 теста |
| [101-rooms-multi-site-by-project.md](./101-rooms-multi-site-by-project.md) | 🏢 Импорт «Помещения» — Site НЕ выбирается в UI, резолвится per-row внутри Project по `(ConstructionProjectNumber, StageNumber)` через `listview/constructionsite/onetomany/Project` с Filter. Pre-pass на уникальные пары (НПС, Этап); 0 match → `site_not_found_in_project`, >1 → `site_ambiguous`. Apply группирует валидные строки по `SiteId`; pre-pass per-site (snapshot/секции/developer); `sectionCache` ключ `(SiteId,Title)`; Parallel.ForEachAsync по `(SiteId, Sheet, Section)`. РНС больше не блокирует (раз site однозначно резолвлен) — PATCH РНС в Site сохраняется в Apply, per-site. UI: prop `showSiteSelect`, `requiresSite = importType !== 'rooms'`. `SiteColumns` дополнен `StageNumber`. |
| [100-finmodel-companygroup-link.md](./100-finmodel-companygroup-link.md) | 🏢 Финмодель → привязка Организации-Застройщика к Группе компаний (поле `Group` в `organization`). Дополнение к [doc 99](99-finmodel-organization-link.md): после поиска/создания организации, если у неё ещё нет `Group`, читаем «Группа компаний» из E14 (C14 — лейбл), ищем `listview/companygroup` с `Filter ["Title","=",X]`. 1 match → PATCH `/crud/organization/{id}` с `Group:{ID,Title,Hidden:false}`. 0 или >1 — row-error «ГК не найдена, тк …» и продолжение без PATCH-а. Новый механизм `SingleValueOverride` в `KeyValueVertical`-парсере (значение лежит не в колонке этапа, а в фиксированной — E вместо H). Колонка опциональна (одиночная, без пары). **v1.1**: `RowError` расширен опциональными `SourceRowNumber`/`Sheet` — ГК-ошибки (и `organization_link_error` / `indicator_*` / `visary_*`) привязываются к params-строке, фронт рендерит их внутри таблицы листа, а не в блоке «Ошибки уровня файла». 5 тестов + asserts на row-level binding |
| [105-control-value-ref.md](./105-control-value-ref.md) | 🪞 `ControlValueRef(SheetName, KeyColumn, ValueColumn, ParameterName, OutputKey)` — KV-парсер хинт: вытащить **скаляр** с отдельного листа (обычно «Control») и подставить в `Cells[OutputKey]` каждого ParsedRow. Опциональный (лист/строка нет → молча skip). Введён для FinModel «Номер КД» → «Номер договора» ([doc 104](./104-finmodel-deal-precheck.md) v1.3). Сравнение с `SingleValueOverride`/`StageCountReference`, антипаттерны (дубль с SingleValueOverride, OutputKey совпадает с KV-колонкой). 2 парсер-теста на подстановку + молчаливый skip. |
| [106-rooms-snapshot-revalidation.md](./106-rooms-snapshot-revalidation.md) | 🔄 Rooms snapshot-revalidation против Visary. Дополняет [doc 96](./96-rooms-incremental-parallel-apply.md): hash-match теперь НЕОБХОДИМОЕ, но не ДОСТАТОЧНОЕ условие skip-а. Перед skip-ом проверяем, что `prev.VisaryRoomId` есть в уже-загруженном `roomsInSection` (стоимость 0), и если в snapshot был `VisaryShareAgreementId` — `GetShareAgreementsByRoomAsync(rid)` (per-row, кэшируется в `saByRoomCache` и переиспользуется основным flow). Удалили помещение/ДДУ в Visary → snapshot признаётся устаревшим, метка `Snapshot устарел (...) — пересоздаём` в RowActions, обычный find-or-create восстанавливает сущности. На сетевой ошибке SA-check — считаем snapshot валидным (временный сбой ≠ удаление). 3 теста (skip-by-hash обновлён, +Room-deleted, +SA-deleted). |
| [104-finmodel-deal-precheck.md](./104-finmodel-deal-precheck.md) | 🤝 Финмодель → **ensure-сделка** в проекте по `DocNumber`. `EnsureDealExistsInProjectAsync` ищет deal через `listview/deal/onetomany/ConstructionProject`. **v1.1**: не нашли → CRUD-create (`Title:"-"` — временный костыль). **v1.2**: между «не нашли в проекте» и `CreateDealAsync` — fallback на ГЛОБАЛЬНЫЙ `listview/deal`. Найдена глобально → row-error `deal_in_other_project` (чужой проект, дубликат запрещён) + skip `ApplyParametersAsync`; бюджет/ГФ продолжаются. **v1.3 (2026-05-21)**: «Номер договора» читается с управляющего листа **Control** (поле «Номер КД» в F-колонке) через новый парсер-хинт `ControlValueRef(SheetName, KeyColumn, ValueColumn, ParameterName, OutputKey)` — парсер подставляет значение в `Cells["Номер договора"]` каждого ParsedRow. **LmID полностью убран из flow** (по запросу заказчика): фильтр Visary listview/deal и payload `CreateDealAsync` идут только по `DocNumber`. Алиасы «ID в LM/KK» удалены. Сигнатуры `IListViewClient.GetDeals*` и `DealCreateRequest.LmID` опциональные — на случай возврата. 10+ тестов (+2 парсер-теста на ControlValueRef). |
| [107-visary-token-provider.md](./107-visary-token-provider.md) | 🔐 OIDC refresh_token-flow для Visary: `IVisaryTokenProvider` + `VisaryAuthHandler` (DelegatingHandler ставит Bearer, на 401 — invalidate+retry один раз), `OidcVisaryTokenProvider` (кэш до `exp − 60s`, single-flight через `SemaphoreSlim`, ротация refresh_token через `IVisaryRefreshTokenStore`). Static-провайдер из `.env` остаётся как dev/legacy fallback (включается, когда `Visary__Auth__TokenEndpoint` пуст). authorization_code НЕ подходит для backend (browser-interactive PKCE). Vault-стора — заглушка, SDK подключается отдельным коммитом после согласования с infra. `ExtraTokenRequestParameters` — точка расширения для будущих полей payload'а |
| [108-rooms-studio-flag.md](./108-rooms-studio-flag.md) | 🛏 Импорт «Помещения»: маркеры студии в «Колич. комнат» («с»/«ст»/«студ»/«студия» — case-insensitive, точное слово после Trim — ИЛИ числовой `0`) → `RoomsNumber=0` + `IsStudio=true`. Для Kind=«Квартира» студия больше не даёт `required_missing`. `IsStudio` добавлен в `RoomCreate/PatchRequest` и в `HashedMappedFields` (иначе включение флага после повторного импорта скипнется по hash). 14 unit-тестов `IsStudioMarker` |
| [109-finmodel-prechecks-wbs-and-gf.md](./109-finmodel-prechecks-wbs-and-gf.md) | 🛡️ Финмодель → 2 pre-check'а перед записями в Visary: (1) `listview/wbs/onetomany/ConstructionSite` непустой → заливку XLSX-бюджета пропускаем (ГФ продолжает работать с уже существующей ИСР), info-сообщение `budget_upload_skipped_wbs_exists`; при исключении listview — skip И заливки И ГФ (`budget_upload_precheck_failed`), чтобы не создать дубликат WBS. (2) В `ApplyChapter1ScheduleAsync` PATCH-on-diff убран — существующий `CostItem` за квартал (любой суммы) → строгий skip. Ручные правки в Visary не перетираются. `PatchCostItemAsync` из мапера больше не вызывается (метод в `Visary.Api.Client` оставлен как публичная обёртка). +3 теста бюджета, заменён тест `PatchesExistingOnAmountChange` → `SkipsExistingEvenOnAmountChange`. |
| [110-finmodel-plan-and-fmmodel.md](./110-finmodel-plan-and-fmmodel.md) | 🗂️ Финмодель → второй (опц.) файл с листом «План» + создание сущности `fmmodel` в Visary. POST `/api/imports` принимает `IFormFile? secondaryFile`, `ImportFileSnapshot` хранит 3 опц. поля (`SecondaryRelativePath`/`FileName`/`SizeBytes`), `ImportContext.SecondaryFileRelativePath` пробрасывается на Apply (требует `Include(FileSnapshot)`). Маппер парсит лист «План» (r3=«Год» с forward-fill, r5=«Квартал», скан с C — B=«Сумма»), формирует `{Year}Q{N}` краевые, pre-check `FindFmModelsAsync` по `(Title, ABConstructionSiteID)` внутри scope проекта, POST `/crud/fmmodel` с `Title="Модель из эксель файла"`. `EnsureFmModelAsync` вызывается в начале `ApplyAsync` (до `validRows`-check'а — ортогонально mapped-строкам). Frontend: второй слот `<input>` в `FileUpload` только при `importType==='finmodel'`. Новая мнемоника `fmmodel`, DTO `FmModelRaw`/`FmModelCreateRequest`. **v1.1**: external-link cleanup в `ReadPlanPeriods` — шаблоны заказчика с формулами `file:////…/[XYZ.xls]Sheet` валят ClosedXML «Unable to determine token»; bytes-retry поверх `XlsxParser.StripExternalLinks` (методы сделаны `internal static`, см. doc 81). **v1.2**: `listview/fmmodel` контракт **отличается** от других listview — `Filter` идёт **JSON-строкой** (не нативный массив), `Scope` обязателен (JSON-string с `EntityId=projectId` + `FilterName=ConstructionProject_ConstructionProject_FMModels_IndirectAssociation`), `Columns` — точный набор из 12 полей по HAR UI; иначе 400. Семантика идемпотентности: `Title contains "Модель из эксель файла"` + `ABConstructionSiteID=<siteId>` внутри Scope проекта. Урок: новую listview-сущность — сначала снимать HAR, потом писать клиент. 17 тестов. |
| [111-incoming-jwt-auth.md](./111-incoming-jwt-auth.md) | 🔐 Аутентификация ВХОДЯЩИХ запросов: `Microsoft.AspNetCore.Authentication.JwtBearer` через тот же IdP, что и Visary OIDC. `Auth:Authority` пуст → middleware не регистрируется (legacy/dev), задан → `RequireAuthorization()` на `MapControllers()` + SignalR-хабах. SignalR забирает токен из query `?access_token=…` через `JwtBearerEvents.OnMessageReceived`. Frontend: общий `services/auth.ts` (`setAccessTokenGetter`/`getAccessToken`), `withAuth(init)` в `importsService.ts`, `accessTokenFactory` в `importsHub.ts`. Поэтапная выкатка через `Auth:Authority`. Заглушка `localStorage.getItem('access_token')` в `main.tsx` ждёт замены на `oidc-client-ts` после регистрации UI-client'а в IdP |
| [112-finmodel-version-and-inputdata.md](./112-finmodel-version-and-inputdata.md) | 🗂️ Финмодель → после `fmmodel` каскадом создаются `fmmodelversion` («Версия - Перенос из Эксель») + «Входные данные» (`inputdata`) по каждой паре (Квартал × вид помещения) из листа «План» + link `listview/inputdata/onetomany/FMModelVersion`. Парсер «План» расширен на 3 категории (Квартиры r6-r8 / Нежилые r9-r11 / м/м r12-r14, якорь — Summ-строка «Сумма от продажи …»), материализация всех точек **в парсере** (`XLWorkbook` закрывается в using, lazy-чтение невозможно). Резолв `Code` через `listview/inputdatacode` (Title→ID за сессию, без хардкода ID:20). Идемпотентность каскадом: версия по Title reuse, inputdata дедуп по `(FMPeriod, Code.ID)`. Нулевые периоды отправляются как есть (заказчик), `Percent=0` всегда. **v1.3 (2026-05-26)**: (1) Парсер «Общий график» поддерживает Layout-1 (Репино-Парк) — шапка `A=Тип помещения` + B=«Квартиры/Нежилое/Кладовые/Машиноместа» ВЫШЕ строки «Год», 1 промежуточная помесячная подшапка между «Год» и «Квартал», Amount-строка с генерик-A-текстом «Площадь, кв.м». Layout-2 (Журавли) сохранён через fallback по A-тексту Amount-строки. (2) `fmmodelversion` теперь создаётся НА КАЖДЫЙ импорт с уникальным sequenced Title через `BuildNextVersionTitle` (`Версия - Перенос из Эксель`, `… 2`, `… 3`, …) — заказчик хочет историю переносов. Pre-check inputdata-by-version удалён (новая версия = заведомо пустая). +5 тестов (Layout-1 + 4 unit BuildNextVersionTitle). **v1.4 (2026-05-26)**: pre-check `FindFmModelsAsync` расширен PeriodStart/PeriodEnd-фильтром — на одном сайте могут жить несколько Финмоделей с разными краевыми периодами, нельзя реюзать чужую (инцидент Репино-Парк: 2024-only-модель матчилась под план 2023..2027, inputdata падали как новая версия чужой модели). Сигнатура `FindFmModelsAsync(projectId, siteId, periodStart, periodEnd, ct)` — оба периода опциональные. +1 регрессионный тест `ApplyAsync_PlanFile_ExistingFmModelWithDifferentPeriod_CreatesNew`. |
| [116-resolve-kind-multiword-plural.md](./116-resolve-kind-multiword-plural.md) | 🏠 Импорт «Помещения» — `ResolveKindBySheetName` теперь обрабатывает многословные имена листов («Коммерческие помещения» → «Коммерческое помещение») и расширенные мн.ч.-окончания (`ые→ая`, `ие→ое`, `ия→ие`). До правки листы `Кладовые` и `Коммерческие помещения` целиком пропускались как «исторические снапшоты» — заказчик видел 26 валидных из 33 вместо 33 |
| [117-ai-business-presentations.md](./117-ai-business-presentations.md) | 🎯 Парные бизнес-презентации (MD + одностраничный HTML) об эффекте ИИ для каждого типа импорта. 9 слайдов: контекст → объём → ценность → план/факт → ускорение пользователя → качество → расходы → итоги. Бизнес-формулировки вместо технических терминов (ОКС/ИСР/ГФ/CRUD/layout/pipeline убраны), файлы предоставляют «сотрудники компании» (не заказчики), при незавершённой разработке — 4 признака статуса (бейдж/плашки/прогресс-бар), будущее время в «Что получит бизнес». HTML наследует фирменный стиль `--red: #ef3124`, навигация стрелками/dots/Home/End оффлайн. Действующие: Квартирограмма (релиз), Финмодель (финальная стадия) |
| [115-section-title-composite-separators.md](./115-section-title-composite-separators.md) | 🏗 Импорт «Помещения» — `ExtractNumericPart` теперь сохраняет составные номера корпусов с разделителями `-`/`/`/`\` («литер 1-1» → «1-1», «лит 1/1» → «1/1», «лит 1\1» → «1\1»). Раньше эти формы схлопывались до «1» — два физически разных корпуса писались как один и PATCH-или друг друга при повторном импорте. Регрессии «лит 1.1»→«1.1», «корп 2»→«2», «3.А»→«3» сохранены |
| [119-rooms-share-agreement-no-steal-from-other-room.md](./119-rooms-share-agreement-no-steal-from-other-room.md) | 🔁 Импорт «Помещения» — ДДУ, найденный глобально с тем же бизнес-ключом, но уже привязанный к другому существующему помещению (`Room.ID > 0 && != roomId`), **не угоняется**: создаём новый ДДУ для текущего помещения, в журнале — `ДДУ создан (№…); существующий id=… оставлен у Room.ID=…`. Match-фильтр в strict/loose теперь `Room null || Room.ID <= 0 || Room.ID == roomId.Value` (симметричный — повторный импорт по-прежнему матчит «свой» ДДУ через strict). Orphan-кейс из [doc 76 v1.1](./76-share-agreement-dedup.md) сохранён, лог orphan-ветки переформулирован в «ДДУ найден глобально как orphan (привязан к помещению)» — больше не путается с «угоном». +1 регрессионный тест |
| [120-rooms-share-agreement-listview-lies.md](./120-rooms-share-agreement-listview-lies.md) | 🔁 Импорт «Помещения» — закрыты ещё 2 дыры угона ДДУ ([doc 119](./119-rooms-share-agreement-no-steal-from-other-room.md) защитил только strict/loose). **Дыра 1**: `listview/shareagreement/onetomany/Room?associationId={roomId}` в проде иногда возвращает в этой выборке ДДУ с `Room.ID > 0 && != roomId` — `matched-in-room` ветка доверяла контракту и PATCH-ила. Теперь фильтр `Room null/<=0/==roomId` симметрично strict/loose. **Дыра 2**: `Raw.Room` из listview приходит `null/{ID:0}` для ДДУ, который на самом деле привязан к другому помещению — orphan-фильтр ошибочно принимал. Источник правды — `GET /crud/shareagreement/{id}`: после strict/loose для «orphan»-кандидата вызываем `GetShareAgreementByIdAsync`; если `Room.ID > 0 && != roomId` — reuse отвергаем, CREATE новый ДДУ. CRUD-верификация только для strict/loose, не для matched-in-room (связь уже подтверждена). Сетевая ошибка CRUD GET — не блокирует. `saRejectedOwnedByOtherRoom ??=` в strict-блоке — не теряем владельца из matched-in-room ветки. Воспроизведено на `RoomImport/Пример импорта корявый.xlsx` (один `№ ДДУ=ап1` в трёх разных помещениях). +3 регрессионных теста (32/32 Apply, 388 в suite) |
| [118-rooms-room-number-accept-any-string.md](./118-rooms-room-number-accept-any-string.md) | 🏷 Импорт «Помещения» (Квартирограмма) — `RoomNumber` принимается как есть (включая текст «ПХ-15», «Кладовка-А», «А1»). `ExtractDigitsOnly` удалён, валидация ослаблена до `Trim()`+`required_missing`. Отменяет цифровую нормализацию из [doc 79](./79-rooms-import-validation-and-fileupload-ux.md). Snapshot `BuildKey` и find-existing-room используют `Trim()+ToLowerInvariant()` — устойчивы к не-числовому `roomNumber`; дубли в Visary не плодятся благодаря уникальному `Section×Kind×Number×BuildingSection` ([doc 77](./77-room-uniqueness-building-section.md)). После раскатки — один «толстый» PATCH-Apply на ранее импортированных файлах (cache-miss snapshot'а) |
| [114-shareagreement-global-search-endpoint.md](./114-shareagreement-global-search-endpoint.md) | 🔁 Глобальный поиск ДДУ переехал на отдельный listview-маршрут `POST /api/visary/listview/shareagreementall` (только `FindShareAgreementsAsync`). Per-room-листинг и весь CRUD остались на `shareagreement`. URL и `Mnemonic` в теле намеренно рассогласованы (`shareagreementall` — это маршрут, не сущность); константу в `VisaryMnemonics` не добавляем, `*all`-суффикс зашит inline. Нужен для поиска orphan-ДДУ с `Stage=NULL/Project=NULL` (loose-find из [doc 76](./76-share-agreement-dedup.md) v1.1) — обычный `listview/shareagreement` ограничен видимостью сайта |
| [113-rooms-additional-fields.md](./113-rooms-additional-fields.md) | 🏷 Импорт «Помещения» — 6 опциональных колонок (Вывод / Стоимость ДКП / Сумма на эскроу / Дата ДДУ / ФИО покупателя / ПИН застройщика) пишутся в Room/ShareAgreement без поиска перед CREATE/PATCH. Поля nullable + `WhenWritingNull` (не затирают существующее), включены в `HashedMappedFields` (иначе повторный импорт со значениями скипнется по hash). **v1.1 (2026-05-27)**: «Дата ДДУ» переведена на Excel-serial (`double?`) + расширен список форматов парсера (`dd/MM/yyyy HH:mm:ss` и компаньоны — починен инцидент с прод-файлом «`04/07/2025 00:00:00`»). **v1.2 (2026-05-27)**: добавлен fallback на `MM/dd/yyyy*` (US) после `dd/MM/yyyy*` — однозначные строки вида `11/27/2025` (день > 12) теперь корректно парсятся; неоднозначные (оба ≤12, `04/07/2025`) сохраняют русскую семантику (Jul 4). **v1.3 (2026-05-27)**: 3 фикса после прод-репорта — (1) `ReadString` нормализует whitespace в заголовках (`\n`/`\r\n`/`\t`/двойные пробелы → один пробел), иначе IsWithdrawn/Cost/Date молча оставались пустыми; (2) `apply_failed` теперь row-bound (`SourceRowNumber`+`Sheet`); (3) Visary 500-error message включает усечённое body. **v1.4 (2026-05-27)**: «Дата ДДУ» возвращена на **ISO-строку `yyyy-MM-dd`** (`string?`), v1.1 откатан — реальный payload Visary UI `POST /crud/shareagreement` шлёт `"Date":"2026-05-26"` строкой, числовой Excel-serial не сохраняется. `TryParseExcelDate` теперь конвертирует и serial-в-ячейке, и текстовые форматы в ISO через `ToString("yyyy-MM-dd")`; DTO `ShareAgreementCreate/PatchRequest.Date` — `string?`; Apply — `GetStringOrNull("ShareAgreementDate")`. Урок: контракт поля проверять по реальному payload UI (HAR / Network), не по устной формулировке. **v1.5 (2026-05-27)**: slash-combined header — шаблон заказчика объединяет «Стоимость ДКП, руб,/Сумма депонирования, руб.» одной ячейкой через `,/`; добавлены 4 явные формы в `SaCostAliases` + slash-aware fallback в `ReadString` (сегментируем cell-key по `,/`, не по голому `/` — иначе «Вывод (да/нет)» разорвётся). Без фикса `ShareAgreement.Cost` молча оставался `null` и Visary не получал значение. +1 регрессионный тест. **v1.6 (2026-05-27)**: Visary `POST /crud/room` **молча дропает** `IsWithdrawn` — в payload отправляем `true`, GET потом возвращает `false` (подтверждено логами room 24899/«Квартира 365_1_»). Видимо, CREATE-эндпоинт принимает ограниченный набор полей; PATCH (`forceUpdate=true`) — принимает. Workaround: после блока ShareAgreement (create/PATCH/привязка орфана) делаем **финальный PATCH Room** с `IsWithdrawn` (только при non-null). Идёт ПОСЛЕ SA-операций — привязка ДДУ может триггерить пересчёт полей Room на стороне Visary (`ActiveShareAgreement` и т.п.); PATCH «после» гарантирует фиксацию. +diag-лог `RoomsForm.Apply.Doc113` на каждой строке (видно, что распарсилось до payload). Урок: контракт CREATE ≠ PATCH у Visary — сверять GET после CREATE для критичных полей. |
| [121-security-fixes-appsec-v1.md](./121-security-fixes-appsec-v1.md) | 🔒 Партия фиксов AppSec v1+v2 (2026-06-04): 55+1 находок сканера → план + выполнение. **v2 (CVE-2026-41907)**: транзитивный `uuid@8.3.2` через `@alfalab/hooks` → `npm overrides` принудительно подтягивает `uuid@^14.0.0`; зависимый код использует только `v4()`, который не затронут CVE — формальное закрытие при нулевом фактическом риске. `npm audit` чистый. (1) Pin 21 npm-зависимости через точные версии. (2) Перенос dev-credentials БД из `appsettings.json`/`docker-compose.yml` в `.env` через `${VAR:?...}` (**deny-by-default**, без fallback — иначе уязвимость вернётся). (3) Helper `src/services/apiUrl.ts` (литерал-префиксы `IMPORTS/VISARY/SITES/HUBS`) закрывает 6 SSRF-предупреждений: сканер видит литерал-prefix вместо динамической конкатенации. (4) Helper `src/services/devLog.ts` (`import.meta.env.DEV`-guard) закрывает 35 `unsafe-formatstring` в 9 файлах `services/`+`hooks/` — бонус: меньше information disclosure в prod-bundle. SSRF и unsafe-formatstring — формальные false positive (relative URLs same-origin; template literal ≠ printf), фиксы дают **структурную защиту от будущей регрессии**. `vite.config.ts` — константные format-строки `'... %s ...'` (DEV-guard в Node-времени неприменим). `npx vitest run` — 59/59 passed. **⚠️ Breaking onboarding**: после pull нужно `cp .env.example .env` + проставить пароли, иначе `docker compose up` останавливается с понятным сообщением. |
| [94-finmodel-auto-budget-before-gf.md](./94-finmodel-auto-budget-before-gf.md) | 🔁 Финмодель → автоматическая заливка бюджета перед ГФ Главы 1. **v1.3** [doc 82](./82-visary-file-storage-upload.md): ручная кнопка `kind="budget-upload"` и `POST /api/imports/{id}/budget-upload` удалены — `FinModelImportMapper.ApplyAsync` сам вызывает `BudgetVisaryUploader.UploadAndWaitAsync` (`UploadAsync` + polling `GET /api/visary/crud/typedimportwbs/{id}` каждые 3 сек, дедлайн 5 мин). `ApplyChapter1ScheduleAsync` запускается только при статусах «успеш»/«предупреж». Captive dependency: мапер Singleton, uploader Scoped → `IServiceScopeFactory.CreateScope()` на каждый Apply. **v1.1 (2026-05-20)**: при «Закончен с ошибками» / timeout / сетевом сбое — одна консолидированная row-error (`budget_upload_failed` / `budget_upload_timeout` / `budget_upload_error`) с тремя блоками: что сделано (параметры применены/нет) + причина Visary (статус + `CountErrors`/`CountWarnings` + `typedimportwbs ID`) + «ГФ Главы 1 не созданы». Отдельная `schedule_skipped_budget_failed` упразднена. **v1.2 (2026-05-20)**: фикс «вечного» timeout — `TypedImportWbsRaw.Status` приведено к `JsonElement?` (Visary шлёт string/number/object), классификаторы `IsSuccessStatus`/`IsFailureStatus` работают через `ExtractStatusText` (раскрывает `{Title}/{Name}/{Caption}`), polling каждые 3 сек пишет в Debug `GetRawText()`+`ValueKind`, exception в `catch` логирует тип/текст. **v1.3 (2026-05-20)**: статус-числовой код подтверждён заказчиком (10=Новый, 20=Закончен успешно, 30=Закончен с ошибками, 40=В обработке, 50=Закончен с предупреждением). `IsSuccessStatus={20,50}` / `IsFailureStatus={30}` сначала идут по `TryGetStatusCode` (Number или Object.ID) и только потом fallback на корни слов. `ExtractStatusText(Number)` теперь отдаёт русское название из таблицы (неизвестный код → «Код N»). Инциденты: ID=9442 — DTO JsonException, ID=9443 — текст «50» не матчился корнем «успеш» |

## 🎯 Контекст проекта

**Проект**: Сервис импорта файлов в Visary (Альфа Банк - Управление проектами)

**Технологический стек**:
- **Backend**: .NET 10 Web API + PostgreSQL + SignalR
- **Frontend**: React 18-19 + TypeScript + `@alfalab/core-components`
- **Контейнеризация**: Docker / Docker Compose

**Основной документ архитектуры**: `../import-excel-service-architecture.md`

**UI-прототип**: `../KiloImportService.Web/`

## ⚠️ Важно при работе

1. **Всегда** используй компоненты из `@alfalab/core-components/<имя>` — это оригинальные компоненты Альфа-Банка
2. **Не используй** Tailwind / Material UI / Ant Design — только Alfa-компоненты
3. **Перед** добавлением пропа компоненту — проверь его типы в `node_modules/@alfalab/core-components-<name>/esm/Component.d.ts`
4. **При** ошибках типов — см. документ `01-alfa-core-components-api.md`
